项目规划（精简版）

说明：按优先级顺序列出，方便逐项勾选。完成后把 [ ] 改为 [√]。

- [√] 1. 确定技术栈与依赖（例如：PySide6 / charset-normalizer / sqlite）
- [√] 2. 建立项目骨架（`src/`, `tests/`, `data/`），并加入基础运行/调试命令
- [√] 3. 实现文本解析器（编码检测、分章规则），为常见样例编写单元测试
- [√] 4. 实现全局设置读写（`settings.json`，原子写、debounce），并写测试
- [√] 5. 实现阅读核心（载入 `.txt`、渲染章节为 HTML/CSS、应用样式）
- [√] 6. 实现设置面板（字体/字号/文字色/背景色/热键），支持实时预览并保存
- [√] 7. 实现自动隐藏与热键唤出（鼠标离开计时器、全局热键回退策略），并做手工验证
- [ ] 8. 集成测试与手动 QA（大文件导入、配色可读性、持久化恢复）
- [√] 8. 集成测试与手动 QA（大文件导入、配色可读性、持久化恢复）
- [√] 9. 打包与发布（PyInstaller 或其他），准备简单安装说明
- [√] 10. 可选项：全文索引/搜索、每书进度持久化、导入自定义字体




备注：所有设置放在项目根目录的 `settings.json`（全局）。MVP 优先完成 3、4、5、6、7、8。

交付实现详单（逐项完成可直接得到成品）

- [ ] 11. 完整 GUI 实现（PySide6）
  - 目标：实现主窗口、书库列表、阅读器视图、设置对话、系统托盘集成。
  - 验收：实现窗口能打开书籍、显示章节、响应设置变化；编写控件到 `SettingsController` 的单元测试。

- [ ] 12. 设置面板 UI（实时预览）
  - 目标：在设置对话中实现字体族、字号、行距、文字色、背景色、热键输入与预设主题；实时预览区能反映修改。
  - 验收：设置变化触发 `SettingsController.update_setting` 并持久化（测试用例覆盖），手动验收预览功能。

- [ ] 13. 书库管理与导入（SQLite 可选）
  - 目标：实现批量导入 `.txt`（或拖拽）、显示书名/作者、保存文件路径与元数据；提供导入进度指示。
  - 验收：导入流程有单元测试（小/大文件），导入后的书目可在 UI 中打开并阅读。

- [ ] 14. 每书进度与书签持久化
  - 目标：为每本书保存当前章节与字符偏移、书签列表；存储方案：SQLite（推荐）或 JSON（备选）。
  - 验收：恢复进度的单元测试；在多次打开/关闭后能恢复阅读位置。

- [ ] 15. 搜索与全文索引（可选但推荐）
  - 目标：使用 SQLite+FTS5 为章节建立索引，支持按关键字跳转到章节/位置。
  - 验收：搜索功能有单元测试，能在大文件中快速定位并跳转。

- [ ] 16. 自动隐藏与全局热键（集成）
  - 目标：把 `AutoHideController` 与 `HotkeyManager` 集成到 UI；在设置中允许开启/关闭、修改延迟与热键。
  - 验收：模拟热键/鼠标离开流程的单元测试 + 手工验收（在 Windows 上测试全局热键注册）。

- [ ] 17. 集成测试与手动 QA（完整路径）
  - 目标：端到端测试导入、阅读、设置、隐藏/唤出、持久化、搜索；手工 QA 覆盖配色可读性（对比度检查）与大文件性能。
  - 验收：自动化集成测试通过（`uv run pytest`），手工 QA 检查表已完成。

- [ ] 18. 打包与发布流程
  - 目标：使用 `pyinstaller` 生成 Windows 可执行（单文件/文件夹），撰写打包脚本与发行说明（包含依赖与安装步骤）。
  - 验收：生成可运行的 exe，并在干净的 Windows 环境能启动（或在 VM/测试机上验证）。

- [ ] 19. 文档与用户指南
  - 目标：完善 `README.md`、用户手册（使用说明：导入、设置、常见问题、卸载）、许可说明。
  - 验收：文档能指导非开发者完成安装与常见操作。

- [ ] 20. 发布前检查与发行
  - 目标：跑清单（测试全部通过、打包成功、文档完整、无敏感信息），准备 release 包（zip/installer）与变更日志。  
  - 验收：一键构建脚本能生成发行包并通过基本 smoke 测试。

每项完成标准（必须遵守）：
1. 编写或更新与该项相关的单元/集成测试。
2. 在本地运行 `uv run pytest` 并确保所有测试通过。
3. 在 `TODO.md` 中把该项改为 `[√]` 并在 `product-change-log.md` 添加该项的变更记录（一行简述）。

常用命令（复制粘贴）：
```
uv add pyside6 charset-normalizer pydantic pytest pyinstaller keyboard
uv run pytest -q
uv run python main.py
uv run pyinstaller --onefile main.py
```
