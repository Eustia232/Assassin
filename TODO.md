项目规划（精简版）

说明：按优先级顺序列出，方便逐项勾选。完成后把 [ ] 改为 [√]。

- [√] 1. 确定技术栈与依赖（例如：PySide6 / charset-normalizer / sqlite）
- [√] 2. 建立项目骨架（`src/`, `tests/`, `data/`），并加入基础运行/调试命令
- [√] 3. 实现文本解析器（编码检测、分章规则），为常见样例编写单元测试
- [√] 4. 实现全局设置读写（`settings.json`，原子写、debounce），并写测试
- [√] 5. 实现阅读核心（载入 `.txt`、渲染章节为 HTML/CSS、应用样式）
- [√] 6. 实现设置面板（字体/字号/文字色/背景色/热键），支持实时预览并保存
- [√] 7. 实现自动隐藏与热键唤出（鼠标离开计时器、全局热键回退策略），并做手工验证
- [ ] 8. 集成测试与手动 QA（大文件导入、配色可读性、持久化恢复）
- [√] 8. 集成测试与手动 QA（大文件导入、配色可读性、持久化恢复）
- [√] 9. 打包与发布（PyInstaller 或其他），准备简单安装说明
- [√] 10. 可选项：全文索引/搜索、每书进度持久化、导入自定义字体




备注：所有设置放在项目根目录的 `settings.json`（全局）。MVP 优先完成 3、4、5、6、7、8。
